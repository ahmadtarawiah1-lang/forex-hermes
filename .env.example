# Paper trading only. Never flip these without understanding the risk.
FOREX_HERMES_MODE=paper
FOREX_HERMES_I_ACCEPT_RISK=false

# Optional: enables `reflect.py --llm` (Claude-powered reflection instead of
# the deterministic rule). Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=
